using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace FirstNight
{
    class Program
    {
        static void Main(string[] args)
        {
            //C#不允许使用为赋值的变量
            int chen =0;
            Console.WriteLine(chen);
            Console.ReadKey();
            //.sln是解决方案文件的后缀
            //.csproj是项目文件的后缀，如果直接打开项目文件则编译器会自动生成一个解决方案，
            //Ctrl+F快速查找，Ctrl+H快速替换。
            //C#不允许使用为赋值的变量
            //C#命名是名称以小写字母开头然后从第二个单词起每个单词的首字母大写。
        }
    }
}
